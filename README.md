# automated-gym-class-booking-selenium


## Steps to run the code
- Install venv
- install dependency fom requirements.txt
- create a .env file
- add required keys to it to ensure the credentials are correctly read and set 
